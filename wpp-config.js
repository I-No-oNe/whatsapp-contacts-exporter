// Must run before wa-js loads. wa-js ships analytics on by default.
window.WPPConfig = {
  disableGoogleAnalytics: true,
  deviceName: false,
  poweredBy: null,
  sendStatusToDevice: false,
  syncAllStatus: false,
};
